[package]
name = "monolayers-client"
version = "0.1.0"
edition = "2024"

[dependencies]
monolayers-core = { path = "../monolayers-core" }
serde = { version = "1.0.228", features = ["derive"] }
tokio = { version = "1.50.0", features = ["full"] }
toml = "1.1.0"
